<UserControl x:Class="LogEOT.UI.Views.AnalyzeLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <Border Background="White"
            BorderBrush="#E2E8F0"
            BorderThickness="1"
            Padding="15"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Log Folder:"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Foreground="#334155"
                           Margin="0,0,10,0"/>

                <TextBox x:Name="FolderTextBox"
                         Grid.Column="1"
                         Height="36"
                         Margin="0,0,10,0"
                         Tag="Select Logs Folder..."/>

                <Button Grid.Column="2"
                        Content="Browse"
                        Width="100"
                        Height="36"
                        Background="#0067C0" 
                        Foreground="White"
                        BorderBrush="#0067C0"
                        BorderThickness="1"
                        Cursor="Hand"
                        Click="Browse_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>

            <Grid Grid.Row="1" Margin="0,0,0,10">
                <TextBlock Text="Search Box"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="#0F172A"
                           VerticalAlignment="Center"/>
            </Grid>

            <DataGrid Grid.Row="2"
                      ItemsSource="{Binding Keys}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="True"
                      CanUserDeleteRows="True"
                      GridLinesVisibility="All"
                      HorizontalGridLinesBrush="#64748B"
                      VerticalGridLinesBrush="#64748B"
                      BorderBrush="#475569"
                      BorderThickness="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Search Key (in log)"
                                        Binding="{Binding Key}"
                                        Width="*"/>

                    <DataGridTextColumn Header="Alt Search Key (in log)"
                                        Binding="{Binding AltKey}"
                                        Width="*"/>

                    <DataGridTextColumn Header="Column Name (in Excel)"
                                        Binding="{Binding ColumnName}"
                                        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,15,0,0">
                <Button Content="Reset Table"
                        Width="150"
                        Height="40"
                        Click="Reset_Click"
                        Margin="0,0,12,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="Foreground" Value="#0067C0"/>
                            <Setter Property="BorderBrush" Value="#0067C0"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="ButtonBorder"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="6">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ButtonBorder" Property="Background" Value="#E3F2FD"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="ButtonBorder" Property="Background" Value="#BAE6FD"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="Run"
                        Width="180"
                        Height="40"
                        Background="#0067C0"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="Run_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
