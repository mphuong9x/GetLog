<UserControl x:Class="LogEOT.UI.Views.CompareMacView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600">
    <Border Background="White"
            BorderBrush="#E2E8F0"
            BorderThickness="1"
            Padding="15"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Compare MAC"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="#0F172A"
                       Margin="0,0,0,10"/>

            <StackPanel Grid.Row="1" Margin="0,10,0,0">
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="90"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Folder A:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#334155"/>
                    <TextBox x:Name="FolderABox" Grid.Column="1" MinHeight="36" Margin="0,0,10,0" Tag="Select the first log folder..." IsReadOnly="True" VerticalContentAlignment="Center"/>
                    <Button Grid.Column="2" Content="Browse" MinHeight="36" Background="#0067C0" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BrowseA_Click"/>
                </Grid>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="90"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Folder B:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#334155"/>
                    <TextBox x:Name="FolderBBox" Grid.Column="1" MinHeight="36" Margin="0,0,10,0" Tag="Select the second log folder..." IsReadOnly="True" VerticalContentAlignment="Center"/>
                    <Button Grid.Column="2" Content="Browse" MinHeight="36" Background="#0067C0" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BrowseB_Click"/>
                </Grid>

                <Grid Margin="0,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Options:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#334155"/>
                    <CheckBox x:Name="RecursiveCheckBox" Grid.Column="1" Content="Include subfolders" IsChecked="True" VerticalAlignment="Center"/>
                </Grid>

                <Border Height="1" Background="#E2E8F0" Margin="0,15,0,15"/>

                <Button Content="Compare MAC" Width="180" Height="40" Background="#0067C0" Foreground="White" BorderThickness="0" Cursor="Hand" Click="Compare_Click" HorizontalAlignment="Center" Margin="0,5,0,15"/>

                <TextBlock x:Name="SummaryText" TextAlignment="Center" FontSize="13" Foreground="#0F172A" TextWrapping="Wrap" LineHeight="20"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
